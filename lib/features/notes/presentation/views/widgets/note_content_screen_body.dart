import 'package:flutter/material.dart';

class NoteContentScreenBody extends StatelessWidget {
  const NoteContentScreenBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: null);
  }
}
